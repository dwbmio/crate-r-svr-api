[package]
name = "api-release"
version = "0.1.0"
edition = "2021"

[dependencies]
static-remote = { path = "../static-remote" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
log = "0.4"

[dev-dependencies]
dotenv = "0.15.0"
